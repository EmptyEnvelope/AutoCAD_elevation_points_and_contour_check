# AutoCAD plugin
自动检测高程点定位与等高线吻合度，标记异常等高线例如高程等高距不匹配/漏画等高线/等高线高度数值异常（小数和0值）
Automatically check if elevation points match with contour lines, and mark abnormal contour lines such as mismatched elevation intervals, missing contour lines, or unusual contour heights (like decimals or zero values)
